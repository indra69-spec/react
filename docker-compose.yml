version: '3.8'

services:
  web:
    image: ${DOCKER_HUB_USER}/react-swarm-app:latest
    ports:
      - "8080:80"
    deploy:
      replicas: 3
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
      restart_policy:
        condition: on-failure